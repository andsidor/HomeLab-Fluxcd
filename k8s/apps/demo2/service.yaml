---
apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: demo2
  labels:
    app: demo-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: demo-app
