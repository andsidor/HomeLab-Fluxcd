apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cloudflare

resources:
  - namespace.yaml
  - external-secret.yaml
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/name: cloudflare-tunnel
      app.kubernetes.io/component: edge-network
    includeSelectors: true
